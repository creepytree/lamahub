"""lamahub - web UI to manage Ollama LLM server models.

Deliberately empty: the application lives in ``lamahub.app`` so that importing
the package (e.g. via the ``lamahub`` console script) does not freeze the
env-derived configuration before the CLI has exported its variables.
"""
